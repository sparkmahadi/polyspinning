import React from 'react';

const TrialMachine = ({value}) => {
    return (
        <div className='bg-sky-300 text-center p-2 rounded-lg'><p>{value}</p></div>
    );
};

export default TrialMachine;